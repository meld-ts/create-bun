{
  "name": "my-project",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "scripts": {
    "dev": "bun run --hot ./src/index.ts",
    "test": "bun test src/ --coverage",
    "ts-check": "tsc --noEmit",
    "build": "bun build ./src/index.ts --outdir ./dist"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
